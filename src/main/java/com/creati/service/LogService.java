package com.creati.service;

import java.util.List;

import com.creati.dto.LogDto;

// DB(TODO): Replace in-memory repository with DB-backed implementation.

public class LogService {

	public void save(LogDto dto) {
		// DB(TODO): repository.save(...)
	}

	public List<LogDto> findAll() {
		// DB(TODO): repository.findAll(...)
		return List.of();
	}
}
